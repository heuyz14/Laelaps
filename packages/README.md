# Shared Packages

Status: Placeholder

This directory is reserved for workspace packages shared by future apps.

Expected packages:

- `analytics`: deterministic running metrics and training calculations
- `db`: database clients, schema helpers, and generated types
- `types`: shared TypeScript domain types
- `config`: shared lint, TypeScript, or runtime configuration
- `ui`: shared UI primitives when web and mobile needs are clear

Add packages only when there is real shared code to extract.
