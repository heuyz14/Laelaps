# RunLog AI Web

Status: Placeholder

This directory is reserved for the future web app, expected to be a Next.js application when implementation begins.

Do not add shared domain logic here by default. Move reusable analytics, database types, validation schemas, and UI primitives into `packages/*` when more than one app or service needs them.
